[package]
name = "speechmarkdown-nodejs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
speechmarkdown-rust = { path = "../.." }
napi = { version = "2", features = ["default"] }
napi-derive = "2"
serde_json = "1.0"

[build-dependencies]
napi-build = "2"
